name: Run tests on push
on:
  push:
jobs:
  cache_files:
    name: Cache files
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@08c6903cd8c0fde910a37f88322edcfb5dd907a8 # v5.0.0
      - name: Check if cache already exists
        uses: actions/cache/restore@0400d5f644dc74513175e3cd8d07132dd4860809 # v4.2.4
        id: file_cache
        with:
          path: local_cache
          key: ${{ hashFiles('src/few/files/registry.yml') }}
          lookup-only: true
      - name: Install UV and Python 3.12
        if: steps.file_cache.outputs.cache-hit != 'true'
        uses: astral-sh/setup-uv@d9e0f98d3fc6adb07d1e3d37f3043649ddad06a1 # v6.5.0
        with:
          python-version: '3.12'
      - name: Download files into cache
        if: steps.file_cache.outputs.cache-hit != 'true'
        run: |
          mkdir -p local_cache
          uv run -C cmake.define.FEW_WITH_GPU=BASE \
            few_files fetch --download-dir $(pwd)/local_cache --tag testfile
      - name: Save the cache
        uses: actions/cache/save@0400d5f644dc74513175e3cd8d07132dd4860809 # v4.2.4
        if: steps.file_cache.outputs.cache-hit != 'true'
        with:
          path: local_cache
          key: ${{ hashFiles('src/few/files/registry.yml') }}
  select_matrix:
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{ steps.read-matrix.outputs.matrix }}
    steps:
      - uses: actions/checkout@08c6903cd8c0fde910a37f88322edcfb5dd907a8 # v5.0.0
        with:
          sparse-checkout: |
            .github
      - id: read-matrix
        run: |
          echo matrix=$(jq -c . .github/workflows/tests.matrix.${{ github.event.repository.visibility }}.json) >> $GITHUB_OUTPUT
  install_and_run_tests:
    name: Install and run non-GPU tests
    runs-on: ${{ matrix.os }}
    needs:
      - cache_files
      - select_matrix
    strategy:
      fail-fast: false
      matrix: ${{ fromJson(needs.select_matrix.outputs.matrix) }}
    steps:
      - uses: actions/checkout@08c6903cd8c0fde910a37f88322edcfb5dd907a8 # v5.0.0
        with:
          fetch-depth: 0
          fetch-tags: true
      - name: Restore the cache
        uses: actions/cache/restore@0400d5f644dc74513175e3cd8d07132dd4860809 # v4.2.4
        id: restore_cache
        with:
          path: local_cache
          key: ${{ hashFiles('src/few/files/registry.yml') }}
      - name: List artifacts
        if: steps.restore_cache.outputs.cache-hit == 'true'
        run: |
          ls -R ./local_cache
      - name: Export configuration options to force using prefetched cache of files
        if: steps.restore_cache.outputs.cache-hit == 'true'
        run: |
          echo "FEW_FILE_ALLOW_DOWNLOAD=no" >> "$GITHUB_ENV"
          echo "FEW_FILE_EXTRA_PATHS=$(pwd)/local_cache" >> "$GITHUB_ENV"
      - uses: astral-sh/setup-uv@d9e0f98d3fc6adb07d1e3d37f3043649ddad06a1 # v6.5.0
        with:
          python-version: '${{ matrix.python-version }}'
      - name: Install LAPACKE and pandoc
        uses: ConorMacBride/install-package@3e7ad059e07782ee54fa35f827df52aae0626f30 # v1.1.0
        with:
          brew: lapack pandoc
          apt: liblapacke-dev pandoc
      - name: Export LAPACK pkgconfig path (macOS 13)
        if: matrix.os == 'macos-13'
        run: |
          echo "LAPACK_PKG_CONFIG_PATH=/usr/local/opt/lapack/" >> "$GITHUB_ENV"
      - name: Export LAPACK pkgconfig path (macOS 14 and 15)
        if: matrix.os == 'macos-14' || matrix.os == 'macos-15'
        run: |
          echo "LAPACK_PKG_CONFIG_PATH=/opt/homebrew/opt/lapack/" >> "$GITHUB_ENV"
      - name: Build list of disabled tags
        if: matrix.disabled_tags
        run: |
          echo "FEW_DISABLED_TAGS=${{ matrix.disabled_tags }}" >> "$GITHUB_ENV"
      - name: Enable test duration measurments
        if: matrix.python-version == '3.12' || matrix.python-version == '3.13'
        run: |
          echo FEWTEST_OTHER_ARGS='--durations 0' >> "$GITHUB_ENV"
      - name: Run module tests
        run: |
          uv run python --version
          uv run \
            --extra testing \
            -C cmake.define.CMAKE_PREFIX_PATH=${LAPACK_PKG_CONFIG_PATH} \
            -C cmake.define.FEW_WITH_GPU=OFF \
            -C cmake.define.FEW_LAPACKE_FETCH=OFF \
            -C cmake.define.FEW_LAPACKE_DETECT_WITH=PKGCONFIG \
            coverage run --source=few -m few.tests ${FEWTEST_OTHER_ARGS}
      - name: Run doc tests
        if: matrix.doctest == 'true'
        run: |
          uv run \
            --extra testing --extra doc \
            -C cmake.define.CMAKE_PREFIX_PATH=${LAPACK_PKG_CONFIG_PATH} \
            -C cmake.define.FEW_WITH_GPU=OFF \
            -C cmake.define.FEW_LAPACKE_FETCH=OFF \
            -C cmake.define.FEW_LAPACKE_DETECT_WITH=PKGCONFIG \
            coverage run --source=few -m sphinx.cmd.build \
            -M doctest docs/source docs/build \
            --define nbsphinx_execute=never
      - name: Show coverage results
        run: |
          uv run --extra doc coverage report -m
      - name: Upload coverage results as artifacts
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: coverage-${{ strategy.job-index }}
          path: .coverage
          retention-days: 2
          include-hidden-files: true
          if-no-files-found: error
  compute-coverage:
    runs-on: ubuntu-latest
    needs:
      - install_and_run_tests
    steps:
      - uses: actions/checkout@08c6903cd8c0fde910a37f88322edcfb5dd907a8 # v5.0.0
      - uses: astral-sh/setup-uv@d9e0f98d3fc6adb07d1e3d37f3043649ddad06a1 # v6.5.0
        with:
          python-version: '3.12'
      - name: Retrieve release distributions
        uses: actions/download-artifact@634f93cb2916e3fdff6788551b99b062d0335ce0 # v5.0.0
        with:
          path: coverage_results/
          merge-multiple: false
      - name: Merge coverage files
        run: |
          uv run --extra testing coverage combine coverage_results/*/.coverage
      - name: Export to Cobertura XML
        run: |
          uv run --extra testing coverage xml
      - name: Summary
        uses: irongut/CodeCoverageSummary@51cc3a756ddcd398d447c044c02cb6aa83fdae95 # v1.3.0
        with:
          filename: coverage.xml
          badge: true
          output: both
          thresholds: '40 80'
